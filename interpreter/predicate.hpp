#ifndef PREDICATE_HPP
#define PREDICATE_HPP

#include <vector>

using std::vector;

typedef unsigned char byte;
typedef unsigned int u32;
typedef long long int i64;

const int EOF_ERROR = -1;
const int ADJUSTED_NONLEVER_ERROR = -2;

#endif
